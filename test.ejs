<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
</head>
<body>
    <%quoteslist.forEach(quotes=>{%>

        <!-- <p><%= quotes.title%></p>
        <p><%= quotes.testdescription%></p>
        <p><%= quotes.markdown%></p> -->
        <!-- <div class="card mt-4">
            <div class="card-body">
                <h4 class="card-title"><%= quotes.title%></h4>
                <div class="card-subtitle">
                    <h3><%= quotes.testdescription%></h3>
                </div>
            </div>
        </div> -->

        <!-- <div class="main" style="height: 200px; width:100%; border: black 2px;">
            <h4>Quote Title: <%= quotes.title%> </h4>
            <h3>Quote Text: <%= quotes.testdescription%></h3>
            <h3>Quote MarkDown:<%= quotes.markdown%> </h3>
            <a href="/data" class="btn btn-danger">Delete</a>
        </div> -->

        <div class="card mt-4" style="margin: 20px;">
            <div class="card-body">
                <h2>id:<%= quotes.id%> </h2>
                <h4 class="card-title">Blog Title:<%= quotes.title%> </h4>
                <div class="card-text mb-2">Blog Description: <%= quotes.description%></div>
                <div class="card-text mb-2">Blog Mark Down: <%= quotes.markdown%></div>
                <form action="/data/<%=quotes.id%>?_method=DELETE" method="POST">
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </div>
        </div>
    <%})%>
</body>
</html>